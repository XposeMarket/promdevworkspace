# Prometheus Subagent HQ

Three.js proof-of-concept for a spatial Prometheus subagent interface.

- Walkable 3D operations floor
- Mock subagents with working / thinking / idle / blocked states
- Mobile touch movement + look controls
- Proximity interactions and mock conversations

This is intentionally a standalone demo. The next step is replacing mocked agent state with live Prometheus worker/session data.